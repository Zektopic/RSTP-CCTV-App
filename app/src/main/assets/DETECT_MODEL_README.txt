Place your LiteRT/TensorFlow object detection model here as:

detect.tflite

Recommended:
- COCO-compatible object detector (for person/cat/dog/bird/horse/sheep/cow)
- Quantized model for mobile performance
- Metadata-enabled model if available

Without detect.tflite, motion detection will still work, but person/animal detection will stay unavailable.
